var config = {
    debug: true,
    hostname: "localhost",
    api_url: "http://localhost:3000/",
    port: 9999,
    redis: {
        host: "localhost",
        port: 6379
    }
};

module.exports = config;
